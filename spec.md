我想创建一个脚手架，功能大概是
在当前目录路径，检测到有构建命令，构建成功后，部署到服务器；
要考虑多个构建命令和多个服务需要部署；
dc init 读取 package.json scripts，提供选项选择构建命令数组，选择内置的部署服务数组，这些设置初始化好后，当前项目的目录路径作为 key，缓存配置；
dc start 就执行构建数组命令 1，部署到服务数组，构建命令 2，部署到服务数组，依次类推；
如果再执行 dc init 就重新初始化配置；

整体需求梳理
先把你的需求整理成清晰规格，再给出完整可运行代码

功能清单
dc init
读取当前目录 package.json 的 scripts，列出所有脚本供用户多选，生成构建命令数组
脚手架内置一份服务器列表数组（多个目标服务），交互式让用户勾选需要部署的服务
使用当前项目目录绝对路径作为唯一 key，将这套配置持久化缓存（存在用户本地，不和项目代码放一起，不污染仓库）
重复执行 dc init：覆盖当前项目已缓存的配置

dc start
根据当前目录路径查找缓存配置
执行逻辑：依次遍历构建命令数组
执行第 1 条构建命令 → 成功后，部署到所有选中服务
执行第 2 条构建命令 → 成功后，部署到所有选中服务
某一条构建失败，直接终止流程，不再继续后续构建 & 部署

约束
不再需要项目内 .deployrc.js
服务器清单内置在脚手架源码，项目侧只选择启用哪些服务
缓存全局存储（放在用户 home 目录，跨终端生效）

技术选型新增
inquirer：交互式选择脚本、服务器

DC 前端自动构建部署脚手架【完整规格文档】
一、需求描述

1. 业务背景
   日常前端项目需要本地执行构建命令，构建完成后将产物上传至远端服务器。现有操作需要手动执行构建、手动打开传输工具上传，流程繁琐，容易漏操作、步骤不一致。
   需要开发一套本地 Node.js CLI 脚手架 dc，标准化「构建→部署」流程，实现一键自动化流水线。

2. 核心需求
   无需在业务项目新增配置文件，不污染项目仓库；项目配置持久化在本机用户目录，以项目绝对路径隔离。
   服务器清单统一内置在脚手架源码中，业务使用者仅能交互式勾选已有服务，不能新增自定义服务器。
   支持配置多条构建命令，按选择顺序串行执行；每条构建成功后，并行部署到所有选中服务器。
   任意一条构建命令执行失败，立即终止整条流水线，不再执行后续构建与部署。
   文件传输采用 SFTP 协议，摒弃 rsync，消除服务器额外安装依赖的前置条件。
   提供完善命令集：初始化配置、查看配置、清理配置、列出全部缓存项目、启动流水线。
   支持命令别名简化调用；输入非法命令友好展示帮助信息。
   远端部署路径直接使用服务预设目录，不再自动拼接项目文件夹名称。
   SSH 连接、文件上传增加超时控制，避免长时间挂起。

3. 用户使用流程
   进入前端项目目录 → 执行 dc init 交互式选择构建命令、设置产物目录、勾选目标服务器 → 配置自动缓存 → 使用 dc -s 一键执行「构建 + 并行部署」。
   切换不同项目目录，配置相互独立，互不干扰；重复执行 dc init 覆盖当前项目旧配置。

4. 非需求边界
   不实现远端旧文件自动清理（仅覆盖同名文件）；
   不支持运行时动态新增服务器，服务器列表修改需要更新脚手架源码；
   单次流水线内，多条构建命令串行执行，不并行执行多条构建；同一构建产物可并行部署至多台服务器；
   不提供 Web 图形界面，仅提供命令行交互。

同一台电脑，打开多个终端，分别执行
终端 A：项目A 执行 dc start
终端 B：项目B 同时执行 dc start
✅ 这种场景是允许的，可以同时跑
两个项目互相独立，缓存 key 是各自项目路径，互不干扰；底层没有全局互斥锁限制。
